#pragma once

enum PositionType
{
	NO_POSITION_TYPE = -1,

	POSITION_TYPE_LEFT_WING,
	POSITION_TYPE_RIGHT_WING,
	POSITION_TYPE_CENTER,
	POSITION_TYPE_DEFENSE
};

enum ZoneType {
	ZONE_TYPE_NEUTRAL,
	ZONE_TYPE_HOME,
	ZONE_TYPE_AWAY
};

enum ShotSelectionType
{
	SHOT_SELECTION_GLOVE_HIGH,
	SHOT_SELECTION_GLOVE_LOW,
	SHOT_SELECTION_STICK_HIGH,
	SHOT_SELECTION_STICK_LOW,
	SHOT_SELECTION_FIVE_HOLE,
	SHOT_SELECTION_UPPER_NET,

	NUM_SHOT_SELECTION_TYPES
};

enum ShotType
{
	SHOT_TYPE_FOREHAND,
	SHOT_TYPE_BACKHAND,
	SHOT_TYPE_SLAPSHOT,

	NUM_SHOT_TYPES
};

enum FaceoffZoneType
{
	FACEOFF_ZONE_HOME,
	FACEOFF_ZONE_AWAY,
	FACEOFF_ZONE_NEUTRAL_HOME,
	FACEOFF_ZONE_NEUTRAL_AWAY,
	FACEOFF_ZONE_NEUTRAL_CENTER,
};

// Potentially unused?
enum SkaterType
{
	NO_SKATER = -1
};

enum TeamType
{
	NO_TEAM = -1
};